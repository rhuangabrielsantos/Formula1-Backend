#!/usr/bin/env php
<?php

require_once __DIR__ . "/../vendor/autoload.php";

use Models\Car;

$result = $_SERVER['argv'];

$car = new Car();

$car->newCar($result[1], $result[2], $result[3], $result[4], $result[5]);
